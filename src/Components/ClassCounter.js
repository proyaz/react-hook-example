import React from 'react';
import PropTypes from 'prop-types';

const ClassCounter = props => {
    return (
        <div>
            
        </div>
    );
};

ClassCounter.propTypes = {
    
};

export default ClassCounter;