import React from 'react';

const color = (props) => {
    return (
        <div>
            <div className={props.backgroundName}>This is a color Area!</div>
        </div>
    )
}

export default color;